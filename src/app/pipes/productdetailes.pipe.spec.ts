import { ProductdetailesPipe } from './productdetailes.pipe';

describe('ProductdetailesPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductdetailesPipe();
    expect(pipe).toBeTruthy();
  });
});
