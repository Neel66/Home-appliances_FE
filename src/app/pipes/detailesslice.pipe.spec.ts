import { DetailesslicePipe } from './detailesslice.pipe';

describe('DetailesslicePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailesslicePipe();
    expect(pipe).toBeTruthy();
  });
});
